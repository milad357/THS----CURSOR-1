[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Netlify will auto-detect Next.js, but explicit plugin ensures proper handling
[[plugins]]
  package = "@netlify/plugin-nextjs"

